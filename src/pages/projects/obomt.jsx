export const meta = {
  slug: 'obomt',
  title: 'Outside Broadcast Operations Management Tool',
  description: 'Custom developed Electron.js app to manage an outside broadcast, providing live notifications and alerts on failure of any monitored devices/entities',
  date: '2025-05-31',
};

const OBOMT = () => (
  <div>
    <h2>{meta.title}</h2>
    <p>{meta.description}</p>
    {/* Add more images, text, etc. */}
  </div>
);

export default OBOMT;